// Create blog.mjs that imports the Post class. Create an instance, and call the publish method to simulate posting.
import Post from './post.mjs';

const post1 = new Post('Gen AI', 'Generative AI is new.');
post1.publish();